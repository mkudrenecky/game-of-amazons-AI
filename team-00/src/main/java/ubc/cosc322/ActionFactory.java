package ubc.cosc322;
import java.util.ArrayList;

public class ActionFactory {
    

    public ArrayList<Action> getActions(Board board, Player player){
        ArrayList<Action> actions = new ArrayList<>();
        // Iterate through all queens controlled by the player

        // Generate queen moves for the current queen

        // Generate arrow placements for each queen move

        // Create actions for each combination of queen move and arrow placement
        return actions;
    }
}
